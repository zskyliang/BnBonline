services:
  bnb-game:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bnb-game
    ports:
      - "${PORT:-4000}:4000"
    environment:
      - PORT=4000
      - NODE_ENV=production
    volumes:
      - ./output:/app/output
    restart: unless-stopped
